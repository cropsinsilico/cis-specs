model:
  name: LightModel
  icon: lightbulb-o
  description: Light Model
  driver: GCCModelDriver
  args:
    - example-fakeplant/src/light.c
    - -lm
  inputs:
    - ambient_light
    - canopy_structure
  outputs:
    - light_intensity
