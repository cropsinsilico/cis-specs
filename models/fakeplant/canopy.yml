model:
  name: CanopyModel
  driver: GCCModelDriver
  args: example-fakeplant/src/canopy.cpp
  inputs:
    - plant_layout
    - init_canopy_structure
    - growth_rate
    - time
  outputs:
    - canopy_structure
  icon: umbrella
  description: Canopy Model
